<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup and device-transfer rules.

  Only shared preferences are transferred. Since an explicit <include> list is
  given, the Room index (shelfie.db) is excluded from both cloud backup and
  device-to-device transfer without needing explicit <exclude> entries.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
